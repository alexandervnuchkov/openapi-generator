- lang: shell
  label: 'cURL'
  source: |
    curl -X GET 'https://petstore.swagger.io/v2/pet/findByTags?tags=tag1,tag2' \
         -H 'Authorization: ApiKey YOUR_API_KEY'

- lang: python
  label: 'Python: Requests'
  source: |
    import requests

    url = "https://petstore.swagger.io/v2/pet/findByTags"
    params = {
      'tags': 'tag1,tag2'
    }
    headers = {
      'Authorization': 'ApiKey YOUR_API_KEY'
    }
    response = requests.get(url, params=params, headers=headers)

    print(response.text)

- lang: js
  label: 'Nodejs'
  source: |
    var request = require('request');

    var options = {
      'method': 'GET',
      'url': 'https://petstore.swagger.io/v2/pet/findByTags',
      'qs': {
        'tags': 'tag1,tag2'
      },
      'headers': {
        'Authorization': 'ApiKey YOUR_API_KEY'
      }
    };

    request(options, function (error, response) {
      if (error) throw new Error(error);
      console.log(response.body);
    });

- lang: java
  label: Java
  source: |
    Unirest.setTimeouts(0, 0);
    HttpResponse<String> response = Unirest.get("https://petstore.swagger.io/v2/pet/findByTags")
      .queryString("tags", "tag1,tag2")
      .header("Authorization", "ApiKey YOUR_API_KEY")
      .asString();
